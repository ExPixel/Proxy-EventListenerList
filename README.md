Proxy-EventListenerList
=======================

An event listener list that removes the need for loops when firing events (well loops are used by the list but at least you don't have to deal with them).
